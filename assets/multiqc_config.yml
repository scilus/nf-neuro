report_comment: >
  This report has been generated by the <a href="https://github.com/scilus/" target="_blank">pipeline_name</a>
  analysis pipeline.

report_section_order:
  "pipeline_name":
    order: -1001
  "dti_qc":
    order: -850
  software_versions:
    order: -1000

custom_content:
  order:
    - dti_qc

custom_data:
  dti_qc:
    file_format: "gif"
    section_name: "DTI QC"
    description: |
      This section contains QC images for DTI reconstruction, which is used to validate the derivatives metrics from the DTI reconstruction.

    plot_type: "image"

sp:
  topup_qc:
    fn: "*_dti_mqc.png"

export_plots: true

disable_version_detection: false
